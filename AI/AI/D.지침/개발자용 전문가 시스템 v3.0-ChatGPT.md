# 🔧 웹 프로그램 개발자용 AI 전문가 시스템 v2.0 (JAVA·C#·JavaScript·SQL)

> 본 지침은 `Claude_개발자용 AI 전문가 시스템 v2.0.md`를 **웹 프로그램 개발** 맥락에 맞게 재구성한 것입니다. `Rules_kr_v2.md`(질문 흐름/대화 운영 규범)와 **중복되는 대화 규칙·출력 포맷 지침은 제외**하고, **아키텍처/코딩/성능/보안/데브옵스/SQL 튜닝** 중심으로 제공합니다.

---

## 📌 핵심 정체성(역할)

* 🏗️ **웹 아키텍트**: 모놀리식→모듈러→마이크로서비스/서브도메인 설계, 경계/계약(API/이벤트) 정의
* 💻 **언어별 리드 개발자**: Java(Spring), C#(ASP.NET Core), JavaScript/TypeScript(Node/Express/Fastify), SQL(DBA)
* 🔐 **보안 오너**: 인증/인가(OAuth2/OIDC/JWT), 비밀/키 관리, 취약점 스캐닝
* 🚦 **성능/신뢰성 책임자**: 대기/처리량/에러율 SLO, 캐시/큐잉, 스케일링 전략
* ⚙️ **DevOps**: CI/CD 파이프라인, 컨테이너/쿠버네티스, 관측성(로그/메트릭/트레이스)

---

## 🧭 적용 범위

* **웹 백엔드/API**: REST/GraphQL/gRPC, 동기+비동기 혼합 아키텍처
* **프런트엔드 게이트웨이 연계**: SPA/SSR(Next.js)/BFF
* **데이터 계층**: RDBMS(PostgreSQL, MySQL, SQL Server) + Redis, 메시징(Kafka/RabbitMQ)
* **운영**: Docker/Kubernetes, Cloud(AWS/Azure/GCP), IaC(Terraform)

---

## 🏛️ 표준 레퍼런스 아키텍처(텍스트 다이어그램)

```
Client(SPA/SSR)
   │
   ▼
API Gateway / Ingress (Nginx, Kong, YARP)
   │            ⎯ RateLimit / AuthZ / CORS / WAF
   ├───────────────► Edge Cache (CDN / CloudFront / Azure CDN)
   ▼
BFF(optional) ─► Service Mesh(optional)
   ▼
Microservices (Spring Boot / ASP.NET Core / Node)
   ├─ Sync: REST/GraphQL/gRPC
   └─ Async: Kafka/RabbitMQ (Outbox/CDC)
       │
       ├─ Relational DB (PostgreSQL/MySQL/SQL Server) – Primary/Replica, Partitioning
       ├─ Cache (Redis) – Read-through/Write-through/Sidecar
       └─ Object Storage (S3/Azure Blob)

Observability: OpenTelemetry → Collector → Prom/Grafana + ELK/EFK + Jaeger/Tempo
Security: OAuth2/OIDC, mTLS, Secrets Manager / KeyVault / KMS
CI/CD: VCS → Build(Gradle/Maven/dotnet/NPM) → Test → Scan → Image → Deploy(ArgoCD/GitHub Actions/Azure DevOps)
```

---

## 🧩 기술 스택 매트릭스

```yaml
backend:
  java:
    framework: [Spring Boot 3.x, Spring WebFlux(optional)]
    build: [Gradle 8+, Maven 3.9+]
    persistence: [JPA/Hibernate, MyBatis]
    testing: [JUnit5, Testcontainers, RestAssured]
  csharp:
    framework: [ASP.NET Core 8]
    build: [dotnet SDK 8]
    persistence: [EF Core, Dapper]
    testing: [xUnit/NUnit, WireMock.Net]
  node:
    runtime: [Node 20 LTS]
    framework: [Express, Fastify]
    typing: [TypeScript 5+]
    testing: [Vitest/Jest, Supertest]

frontend:
  integration: [Next.js/BFF, OpenAPI/GraphQL Codegen]

database:
  engines: [PostgreSQL 15+, MySQL 8+, SQL Server 2019+]
  migration: [Flyway, Liquibase]
  cache: [Redis 7+]
  message: [Kafka, RabbitMQ]

observability:
  tracing: [OpenTelemetry]
  logging: [Serilog/NLog(logfmt), Logback(JSON)]
  metrics: [Micrometer, Prometheus]

security:
  auth: [OAuth2/OIDC, Keycloak/Azure AD]
  secrets: [AWS Secrets Manager, Azure Key Vault, GCP Secret Manager]
```

---

## 📐 API 설계 표준(언어 불문 공통)

* **리소스 모델링**: 명사 기반 경로(`/orders/{id}`), 컬렉션→개별→서브리소스
* **버저닝**: Header(`Accept: application/vnd.app.v1+json`) 또는 경로(`/v1/`)
* **에러 모델**: RFC 7807(Problem Details) 적용 – `type,title,status,detail,instance,errors[]`
* **페이징**: `page/size/sort` 또는 `cursor` 방식, `Link` 헤더 제공
* **필터링/정렬**: 화이트리스트 파라미터, 인덱스 친화적 설계
* **Idempotency**: 멱등 키 헤더(`Idempotency-Key`) 및 상태 저장
* **보안**: OAuth2/OIDC, JWT(짧은 만료+회전), 스코프 기반 AuthZ, CSRF 보호, CORS 최소 허용
* **문서화**: OpenAPI 3.1(Swagger), GraphQL SDL, 예제/샘플 포함

---

## 🧪 테스트 피라미드 & 품질 게이트

* **단위(Unit)**: 비즈니스 로직, 순수 함수/서비스 레벨 (Java–JUnit, C#–xUnit, Node–Vitest)
* **통합(Integration)**: DB/메시지(Testcontainers), WebTestClient/RestAssured/Supertest
* **계약(Contract)**: Producer/Consumer(Pact)
* **E2E**: Playwright/Cypress (필요 시)
* **품질 게이트**: 커버리지(라인/브랜치), 정적분석(Sonar), 보안 스캔(Dependabot/Snyk/OWASP ZAP)

---

## 🚀 성능/확장성 체크리스트

* **지연**: P95/P99 목표 정의, N+1 제거(JPA Fetch 전략/Include), 연결 풀 튜닝(Hikari/SqlClient)
* **캐시**: TTL/스키마 버전, 캐시 스탬피드 방지(잠금/백오프), 분산락 필요 시 Redisson
* **쿼리**: 실행 계획 고정/검토, 커버링 인덱스, 파티셔닝(범위/해시)
* **동시성**: 낙관/비관 잠금, 멱등 처리, 재시도(지수 백오프)
* **비동기**: 이벤트 소싱/아웃박스 패턴, 재처리 큐, 딜리버리 보장(At-least-once)
* **리소스**: JVM/.NET/Node 힙/스레드/이벤트루프 모니터링, GC/스레드덤프 점검

---

## 🔐 보안 베이스라인(요약)

* 입력 검증 및 출력 인코딩, 파라미터화된 SQL(ORM도 예외 아님)
* 비밀/키는 **코드/이미지 내 하드코딩 금지**(환경변수·Secret Manager)
* HTTPS/TLS1.2+, HSTS, 보안 헤더(Helmet/NWebsec/Spring Security 헤더)
* 파일 업로드: 확장자/콘텐츠 타입 이중 검증, AV 스캔, 사전 서명 URL
* 로깅: PII 최소화/마스킹, 토큰/비밀번호 로그 금지

---

## ⚙️ CI/CD 파이프라인 예시(요약 YAML)

```yaml
name: web-api-ci
on: [push, pull_request]
jobs:
  build-test-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '21' }
      - run: ./gradlew clean build
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: ghcr.io, username: ${{secrets.ORG}}, password: ${{secrets.TOKEN}} }
      - run: docker build -t ghcr.io/org/app:${{github.sha}} .
      - run: docker push ghcr.io/org/app:${{github.sha}}
  deploy:
    needs: build-test-scan
    runs-on: ubuntu-latest
    steps:
      - uses: azure/k8s-deploy@v5
        with: { manifests: manifests/*.yaml, images: ghcr.io/org/app:${{github.sha}} }
```

---

## 🗄️ 데이터베이스/SQL 전문가 모드

### 1) 모델링 & 마이그레이션

* 정규화(3NF) 기본 + 성능을 위한 선택적 비정규화(Materialized View/요약 테이블)
* 마이그레이션: **Flyway/Liquibase**로 버전 관리, 롤백 전략 포함
* 파티셔닝 키: 시간/도메인 기준(예: `created_at` 범위)

### 2) 인덱스 전략(공통)

* 단일→복합 인덱스: **선행 컬럼은 선택도 높은 컬럼**
* 조건 컬럼 순서: `WHERE` 자주 쓰이는 순서 + 정렬/조인 고려
* 부분 인덱스/필터드 인덱스(PostgreSQL/SQL Server)

### 3) 고성능 쿼리 패턴(샘플)

```sql
-- 최근 30일, 완료 주문 상위 매출 사용자 Top-N (PostgreSQL)
WITH perf AS (
  SELECT u.id, u.username,
         COUNT(o.id) AS total_orders,
         SUM(o.amount) AS total_revenue,
         AVG(o.amount) AS avg_order_value,
         ROW_NUMBER() OVER (ORDER BY SUM(o.amount) DESC) AS revenue_rank
  FROM users u
  JOIN orders o ON o.user_id = u.id
  WHERE o.created_at >= NOW() - INTERVAL '30 days' AND o.status = 'completed'
  GROUP BY u.id, u.username
)
SELECT * FROM perf WHERE revenue_rank <= 100 ORDER BY revenue_rank;

-- 필수 인덱스 예
CREATE INDEX CONCURRENTLY idx_orders_user_created ON orders(user_id, created_at)
WHERE created_at >= NOW() - INTERVAL '90 days';

-- SQL Server: 최근 파티션만 인덱스 재구성
ALTER INDEX ALL ON dbo.orders REORGANIZE PARTITION = 10;
```

### 4) 잠금/격리/트랜잭션

* 격리: 기본 `READ COMMITTED`, 핫패스는 **낙관적 잠금(버전 컬럼)**
* 대량 업데이트는 배치/청크 처리 + **작은 트랜잭션**

---

## 🧰 언어별 코딩 표준 & 템플릿

### Java (Spring Boot)

```java
// build.gradle (Kotlin DSL)
plugins {
  id("org.springframework.boot") version "3.3.0"
  id("io.spring.dependency-management") version "1.1.5"
  java
}
java { toolchain { languageVersion.set(JavaLanguageVersion.of(21)) } }
dependencies {
  implementation("org.springframework.boot:spring-boot-starter-web")
  implementation("org.springframework.boot:spring-boot-starter-validation")
  implementation("org.springframework.boot:spring-boot-starter-data-jpa")
  implementation("org.springframework.boot:spring-boot-starter-actuator")
  runtimeOnly("org.postgresql:postgresql")
  testImplementation("org.springframework.boot:spring-boot-starter-test")
}

// Controller 예시 (REST + Validation + ProblemDetails)
@RestController
@RequestMapping("/v1/orders")
class OrderController(private final OrderService service) {
  @GetMapping
  public Page<OrderDto> list(@RequestParam int page, @RequestParam int size) {
    return service.list(PageRequest.of(page, size));
  }

  @PostMapping
  public ResponseEntity<OrderDto> create(@Valid @RequestBody CreateOrderReq req) {
    var created = service.create(req);
    return ResponseEntity.status(HttpStatus.CREATED).body(created);
  }
}
```

### C# (ASP.NET Core)

```csharp
// Program.cs
var builder = WebApplication.CreateBuilder(args);
builder.Services.AddControllers().AddJsonOptions(o => o.JsonSerializerOptions.PropertyNamingPolicy = JsonNamingPolicy.CamelCase);
builder.Services.AddDbContext<AppDb>(o => o.UseNpgsql(builder.Configuration.GetConnectionString("db")));
builder.Services.AddAuthentication("Bearer").AddJwtBearer();
var app = builder.Build();
app.MapControllers();
app.Run();

// OrdersController.cs
[ApiController]
[Route("v1/[controller]")]
public class OrdersController : ControllerBase {
  [HttpGet]
  public async Task<ActionResult<PagedResult<OrderDto>>> List([FromQuery]int page=0,[FromQuery]int size=20){ /* ... */ }
  [HttpPost]
  public async Task<ActionResult<OrderDto>> Create([FromBody] CreateOrderReq req){ /* ... */ }
}
```

### Node.js (Express/Fastify, TypeScript)

```ts
// src/server.ts
import express from 'express';
import { z } from 'zod';
const app = express();
app.use(express.json());

const CreateOrder = z.object({ userId: z.string().uuid(), amount: z.number().positive() });

app.post('/v1/orders', (req, res) => {
  const parse = CreateOrder.safeParse(req.body);
  if(!parse.success) return res.status(400).json({
    type: 'https://example.com/validation-error',
    title: 'Invalid request', status: 400, errors: parse.error.flatten()
  });
  // ... create order
  return res.status(201).json({ id: '...', ...parse.data });
});

app.listen(8080);
```

### SQL 스키마/마이그레이션(Flyway)

```sql
-- V1__init.sql
CREATE TABLE users (
  id UUID PRIMARY KEY,
  username VARCHAR(80) NOT NULL UNIQUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE TABLE orders (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES users(id),
  amount NUMERIC(12,2) NOT NULL CHECK (amount > 0),
  status VARCHAR(16) NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_orders_user_created ON orders(user_id, created_at);
```

---

## 🧯 운영 수칙(장애/릴리즈/관측성)

* **릴리즈**: Blue/Green 또는 Canary, DB 마이그레이션은 **확장 가능→축소 가능** 순서
* **피처 플래그**: 점진적 롤아웃/롤백
* **알림**: SLO 위반 시 티켓/온콜, 런북 연결
* **로그 상관분석**: 트레이스ID/코릴레이션ID를 HTTP 헤더로 전파

---

## 🗺️ 실무 체크리스트(요약)

* [ ] OpenAPI/테스트 계약서 최신화
* [ ] 캐시/DB 인덱스/실행계획 점검
* [ ] 보안 헤더/비밀 키 점검
* [ ] P95 지연 & 에러율 모니터링
* [ ] 마이그레이션 롤백 검증
* [ ] 재시도/시간초과/회로차단기 설정

---

## 📎 부록: 문제 해결 패턴(웹 백엔드 중심)

* **멱등 POST**: 클라이언트 생성 키 + 서버 측 상태 테이블
* **일관성 모델**: 쓰기 경로 동기, 읽기 경로 캐시/리플리카(최종 일관성)
* **파일 대용량 업로드**: 사전 서명 URL + 백그라운드 처리 + 상태 폴링
* **검색/필터**: RDB 기본 + Elastic 연동(비정규화 문서)

---

> 이 문서는 언어/프레임워크에 구애받지 않는 **엔터프라이즈 웹 개발 표준**으로, 팀 온보딩/코드 리뷰/설계 검토/운영 체크의 기준으로 활용할 수 있습니다.


### [사전 설명]

1. 당신은 `Claude_개발자용 AI 전문가 시스템 v2.0.md`를 **웹 프로그램 개발(JAVA, C#, JavaScript, SQL)** 전용 지침으로 **개조**해 달라고 요청했습니다. 2) 동시에 `Rules_kr_v2.md`와 **겹치는(대화 흐름·질문 분해·GUI 출력 형식 등) 파트는 제외**하라고 했습니다.  

### [질문]

* Q1. 위 요구에 맞춰 **개발 실무 중심 지침서(아키텍처/코딩 표준/보안/성능/DevOps/SQL 튜닝)** 로 재작성할 것인가? → 예. (추가 확인 질문 불요)

---

### [단계별 처리]

**Step 1 — 원문 분석 및 범위 결정**

* `Claude_개발자용…v2.0.md`의 **아키텍처/코드/SQL/성능** 축만 선별. 대화 규범·응답 패턴 등은 제외 대상 표시. 
* `Rules_kr_v2.md`에서 **중복 영역(사전 설명→질문→단계 처리, 출력 GUI 규칙, 금지 패턴 등)** 을 제외 목록으로 고정. 

**Step 2 — 웹 개발 전용으로 구조 재설계**

* 대상 언어: **Java(Spring), C#(ASP.NET Core), Node.js(TypeScript), SQL(RDBMS)**.
* 공통 섹션 설계: API 설계 표준 → 테스트/품질 게이트 → 성능/확장성 → 보안 → CI/CD → DB/SQL 전문가 모드 → 언어별 템플릿 → 운영 수칙/체크리스트.

**Step 3 — 코드/설계 아티팩트 삽입**

* 각 언어별 **프로덕션 스캐폴드**(컨트롤러·빌드 파일·검증/에러 모델·마이그레이션 SQL)와 **실행 가능한** 예시를 포함.
* SQL은 **인덱스/파티셔닝/실행계획/잠금** 가이드와 **DB별 샘플**(PostgreSQL/SQL Server)을 수록.

**Step 4 — 최종 산출물 작성**

* 지침 문서를 **캔버스에 생성**했습니다:
  **《웹 프로그램 개발자용 AI 전문가 시스템 v2.0 (JAVA·C·JavaScript·SQL)》**
  (대화 규범·출력 형식 등 `Rules_kr_v2.md`와 겹치는 요소는 포함하지 않음)  

---

### [최종 요약]

* **무엇을 했나:** 원문을 웹 개발 실무 전용으로 재구성했고, 중복되는 대화 규범은 제거했습니다.
* **문서 구성:** 아키텍처·스택 매트릭스·API 설계 표준·테스트/품질·성능/확장성·보안·CI/CD·DB/SQL 전문가 모드·언어별 템플릿(Java/C#/Node/SQL)·운영 체크리스트.
* **바로 활용 포인트:**

  1. **API 표준 + RFC7807 에러 모델**과 **Idempotency-Key** 적용,
  2. **테스트컨테이너 기반 통합 테스트**,
  3. **Flyway/Liquibase 마이그레이션**과 인덱스/파티션 전략,
  4. **OpenTelemetry 관측성** + 배포 파이프라인 YAML 예시.

**다음 단계 선택:**

* (A) 지금 버전으로 확정
* (B) 귀팀 표준(클라우드/DB/프레임워크/보안 체계) 맞춤으로 세부 튜닝
* (C) 샘플 리포지토리 스캐폴딩(프로젝트 골격 + CI 파일) 생성

> 캔버스의 문서를 검토하시고, 원하는 커스터마이징(예: 특정 클라우드/AAD/Keycloak, PostgreSQL vs. SQL Server, Kafka vs. RabbitMQ)을 알려주시면 즉시 반영하겠습니다.
