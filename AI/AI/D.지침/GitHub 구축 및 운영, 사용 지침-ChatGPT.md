# 🧰 GitHub 구축·운영·사용 “정확한 답변” 사전 지침 v1.0

## 🧭 개요 (Overview)

* 목적

  * ▷ GitHub 관련 질문(구축/운영/사용/역할/연계)에 **한 번에 정확한 답**을 받기 위한 **사전 브리핑 표준 + 체크리스트 + 미니 재현 자료** 제공
  * → “무엇을 미리 고정해야 하는가?”를 역할(Infra/플랫폼/개발/보안/컴플라이언스)별로 정리
* 범위

  * ▷ 배포 형태: GitHub Enterprise Cloud(GHEC, SaaS) / GitHub Enterprise Server(GHES, Self-managed)
  * ▷ 핵심 구성: GitHub Actions, Self-hosted Runners, Packages/Container Registry, Codespaces, SSO(SAML/OIDC/SCIM), Advanced Security(코드스캐닝/비밀탐지/종속성 스캔)

> **Key takeaway: 질문 전에 ‘배포형태·역할·아키텍처·보안정책·Actions·연계’를 템플릿에 고정하면 정답률이 급상승한다.**

---

## 🧱 A. 프레이밍 & 원칙

* Overview

  * ▷ Background

    * GitHub은 **Cloud vs Server, Org/Enterprise 정책, Actions/Runners, 보안 기능(Advanced Security)** 등 변수가 많아 질문이 쉽게 모호해진다.
    * 같은 증상이라도 **GHEC vs GHES**, **Hosted Runner vs Self-hosted**, **Org 정책/Repo 보호 규칙**에 따라 해법이 달라진다.
  * → 원칙

    * **역할·책임(RACI) 고정** → **토폴로지/정책 다이어그램** → **버전·로그·스크린샷 규격** → **재현용 최소 예시** 순서로 준비

> **Key takeaway: “역할/토폴로지/버전/로그/재현 예시” 5종 세트가 선행 데이터다.**

---

## 🧑‍🤝‍🧑 B. 역할 매트릭스 (RACI)

| 기능/결정                          | Infra(시스템) | 플랫폼/GitOps | 개발자 | 보안 | 컴플라이언스 |
| ------------------------------ | ---------- | ---------- | --- | -- | ------ |
| 배포 방식 결정(GHEC/GHES)            | R          | A          | C   | C  | C      |
| GHES 업그레이드/백업/DR               | A          | R          | C   | C  | C      |
| Actions Runner 표준(실행기/스케일링)    | C          | A/R        | C   | C  | C      |
| Enterprise/Org 정책(SSO/SCIM/권한) | C          | R          | C   | A  | A      |
| Repo/브랜치 보호·리뷰 규칙              | C          | R          | A   | C  | A      |
| Packages/Container Registry 정책 | C          | R          | C   | A  | C      |
| 비밀관리/환경(Secrets/Environments)  | C          | R          | C   | A  | C      |
| 감사로그/보존/내보내기                   | C          | R          | C   | A  | A      |

> **Key takeaway: 질문 시 “최종결정자(A)가 누구인지”를 명시하면 판단이 빨라진다.**

---

## 🧩 C. 사전 브리핑 템플릿 (복붙 사용)

**아래 블록을 채워서 질문 앞에 붙이면 정확도가 확 올라간다.**

### 1) 배포/버전/에디션

* 배포: `GHEC / GHES`
* (GHES일 때) 버전: `X.Y` / 라이선스 좌석: `N seats` / 만료: `YYYY-MM-DD`
* Enterprise/Org 구조: `Enterprise 슬러그 / Org 이름(들)`

### 2) 인프라/네트워크

* (GHES) 하이퍼바이저/클라우드: `VMware / AWS / Azure / GCP / 베어메탈`
* OS/노드 스펙: `vCPU/RAM/스토리지(IOPS)`
* 네트워크: `프록시/방화벽/아웃바운드 제한, 포트(22/443/registry 등)`

### 3) 데이터/스토리지

* GHES 외부 스토리지(객체/SAN) 여부, 스냅샷/백업 주기, RPO/RTO
* Packages/Container Registry 용량/보존 정책

### 4) 인증/권한

* IdP/SSO: `SAML/OIDC` + SCIM 프로비저닝 여부
* Org/Team/Role 정책(Owner/Maintainer/Write/Read)
* 브랜치 보호 규칙: `main 보호, 필수 리뷰 2, status checks 필수`

### 5) Actions & Runners

* Runner 유형: `GitHub-hosted / Self-hosted / ARC(K8s)`
* 토폴로지: `Enterprise/Org/Repo 단위`, 라벨/동시성/오토스케일
* 워크플로 권한: `GITHUB_TOKEN 권한(읽기/쓰기)`, 권한 상향 정책

### 6) 연계

* Kubernetes/ARC(actions-runner-controller), Artifactory/Harbor, Jira/Slack
* OIDC로 클라우드 권한 연동(AWS/GCP/Azure) 사용 여부
* GitHub Apps/Webhooks 목록(이벤트/시크릿/타임아웃)

### 7) 증상/요구/재현

* 에러 메시지 전문 + 타임스탬프(KST)
* 최근 변경(업그레이드/정책/권한)
* 재현 절차(1-2-3) + 스크린샷 경로(예: **Settings → Actions → Runners**)

> **Key takeaway: 위 7개 블록이 채워지면 정확한 트러블슈팅이 가능하다.**

---

## 🏗 D. 아키텍처 선택 가이드 (의사결정 트리)

* ▷ Preparation

  * 사용자 수/동시 Actions 잡 수/이미지 크기/규제(온프레미스 필수?) 명확화
* ▷ Procedure

  * → Step 1: **GHEC**(운영 부담 최소)

    * 적합: 빠른 온보딩, GitHub-hosted Runner 활용, 글로벌 협업
  * Step 2: **GHES**(자체 호스팅/규제 충족)

    * 적합: 데이터 주권/망분리, 자체 Runner·Registry·감사 로그 통제
  * Step 3: **하이브리드**(GHEC + 온프레 Runner/프록시)

    * 적합: Cloud 기능 + 온프레 자원 격리/캐시/성능

> **Key takeaway: 규제·규모·Actions 실행환경 3요소로 GHEC↔GHES를 결정한다.**

---

## 🧪 E. 미니 재현 패키지 (필수 동봉)

### 1) 최소 워크플로 예시 `.github/workflows/ci.yml`

```yaml
name: ci
on:
  push:
    branches: [ main ]
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show context
        run: |
          echo "Actor: ${{ github.actor }}"
          echo "Ref:   ${{ github.ref }}"
      - name: Run tests
        run: echo "CI on $(date)"
```

### 2) Self-hosted Runner 등록 (Linux, Docker 작업용)

```bash
# 1) 런너 디렉터리 준비
mkdir actions-runner && cd actions-runner
curl -o actions-runner.tar.gz -L https://github.com/actions/runner/releases/download/v2.319.1/actions-runner-linux-x64-2.319.1.tar.gz
tar xzf actions-runner.tar.gz

# 2) 등록 (GitHub UI의 Repo/Org/Enterprise → Settings → Actions → Runners → New runner)
./config.sh --url https://github.com/<org>/<repo> --token <REG_TOKEN> \
  --labels docker --runnergroup Default

# 3) 서비스로 실행
sudo ./svc.sh install
sudo ./svc.sh start
```

### 3) Windows Self-hosted Runner (PowerShell)

```powershell
mkdir actions-runner; cd actions-runner
Invoke-WebRequest -Uri https://github.com/actions/runner/releases/download/v2.319.1/actions-runner-win-x64-2.319.1.zip -OutFile runner.zip
Expand-Archive runner.zip -DestinationPath .
.\config.cmd --url https://github.com/<org>/<repo> --token <REG_TOKEN> --labels windows
.\run.cmd
```

### 4) 스크린샷 규격

* **경로 텍스트 포함**(예: *Settings → Actions → Runners* / *Settings → Branches → Branch protection rules*)
* 에러 토스트/로그 패널/Run ID가 보이도록 **전체 창**
* 트리거 커밋 SHA/워크플로 Run 링크 포함

> **Key takeaway: ‘최소 워크플로 + Runner 등록 로그 + 스크린샷’ 3종이 재현 핵심.**

---

## 🔐 F. 보안·권한·컴플라이언스 고정안

* Overview

  * ▷ Background: 문제의 상당수는 **브랜치 보호/필수 체크/권한/비밀/토큰 만료** 미설정에서 발생
* Baseline

  * → 브랜치 보호: `main` 보호, **필수 리뷰(2명)**, **필수 상태 체크(워크플로 성공)**
  * → CODEOWNERS로 경로별 리뷰어 강제
  * → 환경(Environments) + **승인자 보호**로 프로덕션 배포 통제
  * → **Secrets/Variables**: Org/Repo/Environment 범위, **만료/회전**, **마스킹**
  * → OIDC + 클라우드(aws sts:AssumeRole 등)로 **키리스(Keyless)** 접근
  * → Advanced Security: 코드스캐닝(SAST), Dependabot/종속성 리뷰, 비밀탐지
  * → 감사: **Audit log 내보내기**, 보존기간, SIEM 연계
* GUI 기준 예:

  * *Organization Settings → Code security and analysis*
  * *Repository Settings → Branches → Add rule*
  * *Settings → Secrets and variables → Actions*
  * *Environments → Production → Required reviewers*

> **Key takeaway: 브랜치 보호·CODEOWNERS·환경 승인·키리스 OIDC·감사 로그가 기본 안전장치.**

---

## 🧵 G. 연계(Integration) 사전체크

* Kubernetes: **actions-runner-controller(ARC)**, 네임스페이스/RBAC/오토스케일
* Registry: GitHub Packages/Container registry, 보존·서명(Cosign) 정책
* SSO/SCIM: SAML/OIDC, 그룹 매핑(팀 ↔ IdP), JIT/SCIM 프로비저닝
* Chat/ALM: Slack(워크플로 알림), Jira(이슈 연동), Webhooks(서명 secret/재시도)
* 클라우드 권한: OIDC 신뢰정책, 최소 권한 역할, 세션 만료

> **Key takeaway: 연계는 ‘자격·네트워크·보존’ 3요소를 템플릿에 고정해야 한다.**

---

## ⚙️ H. 운영 표준(Backup/Upgrade/DR/모니터링)

* GHEC: 데이터 보존·감사 로그 내보내기·Export 정책 정립
* GHES:

  * Backup/Restore: **ghe-backup / ghe-restore** 표준화, **복구 리허설** 분기 1회
  * Upgrade: **Staging → Prod 순차**, 릴리즈 노트/호환성(Runner/앱) 확인
  * DR: RPO/RTO 목표, 스냅샷/오브젝트스토리지 복제
* 모니터링: Actions 성공률/대기열/실패율, Runner 가용/동시성, Registry 용량 경보
* 캐시 전략: Actions 캐시/아티팩트 보존일, 대용량 의존성 프록시

> **Key takeaway: GHES는 ‘분리 백업·복구연습·사전 호환성’이 핵심 운영 안전장치.**

---

## 🧑‍💻 I. 팀별 미니 제출 템플릿(개발/플랫폼 공용)

* Repo 링크/가시성(Private/Internal/Public)
* 브랜치 전략(Git-Flow/Trunk), **브랜치 보호 규칙**
* 머지 규칙(PR 템플릿/승인자/필수 체크)
* Actions 권한(GITHUB_TOKEN/워크플로 권한), Secrets 목록(범위/만료/마스킹)
* 베이스 이미지/캐시/아티팩트 보존일
* 실패 로그(Workflow Run 링크/Run ID/Runner 라벨/최근 커밋 SHA)

> **Key takeaway: 개발팀은 “브랜치/승인/체크/시크릿/이미지/아티팩트”를 표로 제출.**

---

## 🧭 J. 문제 유형별 “필수 질문 리스트”

* Actions 잡이 대기만 한다

  * → Runner 온라인/라벨 일치?, 동시성 제한?, 큐 길이/스케일러 동작?
* OIDC로 클라우드 접근 실패

  * → IdP 신뢰 정책/감사 로그, audience/subject 클레임, 역할 권한 최소화 확인
* PR이 머지되지 않음

  * → 브랜치 보호 조건(필수 체크/리뷰 수), CODEOWNERS 매칭, Dismissal 설정
* Registry 푸시 실패

  * → 토큰 권한/만료, 포트/프록시, 레이어 재시도/스토리지 임계치, 네임스페이스 권한

> **Key takeaway: 증상별 체크 3~4개만 확인해도 대부분 원인이 좁혀진다.**

---

## ✅ 체크리스트

1. GHEC/GHES 결정 및 버전/라이선스/구조(Enterprise/Org) 고정
2. 네트워크/스토리지/백업/DR 명시(GHES)
3. SSO/SCIM/권한·팀 모델/브랜치 보호·필수 체크 고정
4. Actions Runner 토폴로지/라벨/동시성/오토스케일 고정
5. **최소 워크플로** + Runner 등록 로그 + 스크린샷 첨부
6. 연계 대상(K8s/Registry/Jira/Slack/Cloud OIDC)과 보존·자격 정책 명시
7. 에러 전문/타임스탬프/Run 링크/커밋 SHA 포함
8. 업그레이드 전 호환성 & 백업 검증(특히 GHES)
9. 모니터링 대시보드(성공률/대기열/용량) 링크 첨부

---

## 📎 부록: 바로 쓸 수 있는 템플릿(요약판)

```
[환경]
- 배포: GHEC / GHES(버전 X.Y, 좌석 N, 만료 YYYY-MM-DD)
- 인프라/네트워크: VM/클라우드, 프록시/포트, 스토리지/백업/DR
[보안/권한]
- IdP/SSO/SCIM, 팀·역할, 브랜치 보호/필수 체크, CODEOWNERS
- Secrets/Vars(범위/만료/마스킹), OIDC 클라우드 역할
[Actions & Runners]
- Runner 유형/라벨/동시성/오토스케일, 최소 워크플로/등록 로그
[연계]
- K8s(ARC), Registry(보존/서명), Jira/Slack/Webhooks, Cloud OIDC
[증상/요구/재현]
- 메시지 전문/시간/최근 변경, 재현 절차(1-2-3) + 스크린샷 경로
```

> **Note:** 위 템플릿을 질문 앞에 붙여주면 “원인/해결/대안”을 즉시 제시할 수 있다.

---

## 🧾 마지막 한 줄 요약

> **역할·정책·Actions·Runner·연계를 템플릿으로 ‘고정 제출’하면 GitHub 질문 정답률이 폭증한다.**
