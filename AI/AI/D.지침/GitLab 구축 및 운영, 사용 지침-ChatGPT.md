# 🧰 GitLab 구축·운영·사용 “정확한 답변” 사전 지침 v1.0

## 🧭 개요 (Overview)

* 목적

  * ▷ GitLab 관련 질문(구축/운영/사용/역할/연계)에 **한 번에 정확한 답**을 받기 위한 **사전 브리핑 표준 + 체크리스트 + 미니 재현 자료** 제공
  * → “무엇을 미리 고정해야 하는가?”를 역할(Infra/DevOps/개발/보안/컴플라이언스)별로 정리
* 범위

  * ▷ 배포 형태: GitLab.com(SaaS), Self-Managed(Omnibus/Docker/K8s Helm)
  * ▷ 연계: Runner, Registry, Kubernetes, SSO(LDAP/SAML/OIDC), 이슈 트래커(Jira), ChatOps(Slack) 등

> **Key takeaway: 질문 전에 ‘환경·역할·아키텍처·보안·CI 파이프라인·연계’를 표준 템플릿에 고정하면 정답률이 급상승한다.**

---

## 🧱 A. 프레이밍 & 원칙

* Overview

  * ▷ Background

    * GitLab은 **배포 방식/에디션/버전/라이선스/런너/스토리지/네트워크** 변수가 많아 질문이 모호해지기 쉽다
    * 같은 증상이라도 **SaaS vs Self-Managed**, **Omnibus vs Helm**, **Runner 실행기(Docker/Machine/Shell)** 등에 따라 해결책 상이
    * GUI 기준의 정확한 경로(예: **Admin Area → Settings → CI/CD → Runners**)와 메시지 전문이 중요
  * → 원칙

    * **역할·책임(RACI) 고정** → **아키텍처/토폴로지 도식** → **환경·버전·로그·스크린샷 규격** → **재현용 최소 예시** 순서로 준비

> **Key takeaway: “역할/토폴로지/버전/로그/재현 예시” 5종 세트가 선행 데이터다.**

---

## 🧑‍🤝‍🧑 B. 역할 매트릭스 (RACI)

| 기능/결정                  | Infra(시스템) | DevOps(플랫폼) | 개발자 | 보안 | 컴플라이언스 |
| ---------------------- | ---------- | ----------- | --- | -- | ------ |
| 배포 방식 결정(SaaS/On-prem) | R          | A           | C   | C  | C      |
| 업그레이드/백업/DR            | A          | R           | C   | C  | C      |
| Runner 표준(실행기/스케일링)    | C          | A/R         | C   | C  | C      |
| SSO/권한/그룹 정책           | C          | R           | C   | A  | A      |
| 프로젝트/브랜치/승인 규칙         | C          | R           | A   | C  | A      |
| 컨테이너 레지스트리/이미지 서명      | C          | R           | C   | A  | C      |
| 비밀관리/변수 보호             | C          | R           | C   | A  | C      |
| 감사로그/보존/내보내기           | C          | R           | C   | A  | A      |

> **Key takeaway: 질문할 때 “누가 A(최종결정)인지”를 명시해야 답이 명확해진다.**

---

## 🧩 C. 사전 브리핑 템플릿 (복붙 사용)

아래 블록을 채워서 질문 앞에 붙이면 정확도가 확 올라간다.

### 1) 배포/버전/에디션

* 배포 형태: `GitLab.com / Self-Managed(Omnibus|Docker|Helm on K8s)`
* 버전: `XX.Y`
* 에디션: `Free / Premium / Ultimate`
* 라이선스 좌석 수/만료일: `N seats / YYYY-MM-DD`

### 2) 인프라/네트워크

* 인프라: `VM/베어메탈/클라우드(AWS|GCP|Azure)`
* OS/커널: `Ubuntu 22.04 / …`
* 네트워크: `프록시/방화벽/아웃바운드 제한 여부, 포트 오픈(22/443/5050/registry 5050 등)`

### 3) 데이터/스토리지

* 외부 DB/Redis 사용 여부(URI/버전)
* 아티팩트/레지스트리/패키지 저장소 용량/백업 정책
* 스냅샷/DR:RPO,RTO 목표

### 4) 인증/권한

* SSO: `LDAP/SAML/OIDC`
* 그룹/프로젝트 구조(상위그룹·서브그룹)
* 역할 정책: `Maintainer 이상만 Protected Branch push` 등

### 5) CI/CD & Runner

* Runner 토폴로지: `Shared/Group/Specific`, 실행기: `docker|shell|machine|kubernetes`
* 태그/컨스트레인트, 동시성·오토스케일
* `.gitlab-ci.yml` 최소 예시(아래 예제 포함)

### 6) 연계

* Kubernetes 연결(에이전트/클러스터ID), Ingress/Service 타입
* Registry/Harbor/署名(Cosign), SBOM/보안스캔 사용 여부
* Jira/Slack/Webhook URL·이벤트 목록

### 7) 증상/요구/재현

* 증상 메시지 전문 + 타임스탬프(KST)
* 최근 변경(업그레이드/설정 수정)
* 재현 절차(1-2-3 단계) + 결과 스크린샷 규격(경로·전체 창/텍스트)

> **Key takeaway: 위 7개 블록이 채워지면 트러블슈팅 답변이 바로 가능하다.**

---

## 🏗 D. 아키텍처 선택 가이드 (의사결정 트리)

* ▷ Preparation

  * → 요구: 사용자 수/동시 CI 잡 수/이미지 크기/규제(온프레미스 필수?)
* ▷ Procedure

  * → Step 1: **SaaS GitLab.com** (운영 부담 최소)

    * 적합: 소규모/빠른 온보딩/규제 낮음
  * Step 2: **Self-Managed Omnibus** (단일 호스트 빠른 구축)

    * 적합: 중소 규모/간단 인프라
  * Step 3: **Helm on K8s** (확장성/격리/오토스케일)

    * 적합: 중대규모/플랫폼 표준화/멀티런너

> **Key takeaway: 규제·규모·런너 확장성 3요소로 SaaS↔On-prem을 결정한다.**

---

## 🧪 E. 미니 재현 패키지 (필수 동봉)

* `.gitlab-ci.yml` 최소 실행 예시(테스트용)

```yaml
stages: [test]
test:  
  stage: test
  tags: [docker]
  image: alpine:3.20
  script:
    - echo "CI on $(date)"
```

* Runner 등록 커맨드(예: Docker 실행기)

```bash
docker run -d --name gitlab-runner --restart always \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v gitlab-runner-config:/etc/gitlab-runner \
  gitlab/gitlab-runner:alpine

docker exec -it gitlab-runner gitlab-runner register \
  --url https://<your-gitlab-url>/ \
  --registration-token <TOKEN> \
  --executor docker \
  --docker-image alpine:3.20 \
  --description "shared-docker" \
  --tag-list "docker" \
  --locked=false
```

* 스크린샷 규격

  * **경로 텍스트 포함**(예: *Admin Area → Settings → CI/CD → Runners*)
  * 에러 토스트/배지/로그 패널이 보이도록 **전체 창** 캡처
  * 시간/브랜치/잡ID가 보이게

> **Key takeaway: ‘최소 .gitlab-ci.yml + Runner 등록 로그 + 스크린샷’ 3종이 재현 핵심.**

---

## 🔐 F. 보안·권한·컴플라이언스 고정안

* Overview

  * ▷ Background: 질문의 30%는 권한/토큰/보호 브랜치/변수 보호 미설정에서 발생
* Baseline

  * → 그룹/프로젝트 구조 표준(상위그룹에 규칙 상속)
  * → Protected Branch: `main`은 **Maintain­er+만 push**, 머지엔 **MR 승인(2인)**
  * → CI/CD Variables: **Protected/Masked** 플래그, 환경별 스코프
  * → 토큰: **만료일 필수**, 범위 최소(OIDC/JWT 권장)
  * → 감사: **Audit Events Export** 주기적 수집, 보존기간 명시

> **Key takeaway: Protected Branch·승인·Protected 변수·만료 토큰 4종은 기본 안전장치.**

---

## 🧵 G. 연계(Integration) 사전체크

* Kubernetes: 에이전트, 네임스페이스/SA/권한(RBAC), Ingress/LoadBalancer
* Container Registry: 용량/보존/프라이빗/서명(Cosign)/프록시
* 패키지 레지스트리: 언어별(NPM/PyPI/Maven) 토큰 스코프
* SSO: LDAP/SAML/OIDC 매핑(그룹 → GitLab Role), JIT 프로비저닝
* 외부 ALM: Jira(프로젝트 키/워크플로), Slack(이벤트/서명검증)
* Webhook: 이벤트 타입, 재시도/서명(secret), 타임아웃

> **Key takeaway: 연계는 ‘자격·네트워크·보존’ 3요소를 템플릿에 고정해야 한다.**

---

## ⚙️ H. 운영 표준(Backup/Upgrade/DR/모니터링)

* Backup: 레포/DB/레지스트리/아티팩트 분리 백업, **복구 리허설** 분기 1회
* Upgrade: **Staging → Prod 순차**, 버전 호환(Runner/Helm 차트), 릴리즈 노트 링크
* DR: RPO/RTO, 외부 객체스토리지(예:S3) 스냅샷, 레지스트리 복제
* 모니터링: Prometheus + Grafana, Runner 동시성/대기열/실패율, 레지스트리 용량 경보

> **Key takeaway: 백업은 ‘분리·주기·복구연습’, 업그레이드는 ‘사전호환성’이 핵심.**

---

## 🧑‍💻 I. 사용 가이드(개발팀 제출용 미니 템플릿)

* 프로젝트 링크/가시성(Private/Internal/Public)
* 브랜치 전략(Git-Flow/Trunk), 보호 브랜치 목록
* 머지 규칙(MR 템플릿/승인자/파이프라인 필요)
* CI 변수 목록(이름/범위/보호/마스킹)
* 실행 환경(베이스 이미지/캐시/아티팩트 보존일)
* 실패 로그(잡ID/스테이지/러너 태그/최근 커밋 SHA)

> **Key takeaway: 개발팀은 “브랜치/승인/변수/이미지/아티팩트”를 표로 제출.**

---

## 🧭 J. 문제 유형별 “필수 질문 리스트”

* Runner가 안 잡힌다

  * → 런너 등록 상태(온라인/오프라인), 태그 일치 여부, 실행기 이미지 Pull 가능?, Job 로그 첫 50줄
* 파이프라인이 느리다

  * → 동시성/Autoscale, 캐시/아티팩트 크기, 레지스트리 네트워크 레이턴시, 테스트 병렬화 여부
* 레지스트리 푸시 실패

  * → 포트/프록시/자격증명 만료, 레이어 재시도, 스토리지 사용량 임계치
* SSO 그룹 권한 오작동

  * → 매핑 규칙/상속 구조/SCIM 여부/강제 동기화 로그

> **Key takeaway: 증상별 체크 3~4개만 확인해도 대부분 원인 좁혀진다.**

---

## ✅ 체크리스트

1. 배포 방식/버전/에디션을 템플릿에 고정
2. 네트워크/스토리지/DB/Redis/백업 정책 명시
3. SSO/권한/보호 브랜치/승인 규칙 고정
4. Runner 토폴로지/태그/실행기/동시성 고정
5. `.gitlab-ci.yml` **최소 예시** + 등록 로그 첨부
6. 연계 대상(K8s/Registry/Jira/Slack)과 자격·보존 정책 명시
7. 에러 메시지 전문/타임스탬프/스크린샷 규격 지킴
8. 업그레이드 전 호환성 & 백업 검증
9. 모니터링 대시보드(성공률/대기열/용량) 링크 첨부

---

## 📎 부록: 바로 쓸 수 있는 템플릿(요약판)

```
[환경]
- 배포/버전/에디션:
- OS/클라우드/네트워크(프록시/포트):
- DB/Redis/스토리지/백업:
[보안/권한]
- SSO(LDAP/SAML/OIDC) & 그룹 정책:
- Protected Branch & 승인 규칙:
- 토큰/변수(Protected/Masked/만료):
[CI/CD & Runner]
- Runner(Shared/Specific, 실행기, 태그, 동시성):
- 최소 .gitlab-ci.yml & 등록 로그:
[연계]
- K8s(에이전트/Ingress), Registry(서명/보존), Jira/Slack/Webhook:
[증상/요구/재현]
- 메시지 전문/시간/최근 변경:
- 재현 절차(1-2-3) + 스크린샷:
```

> **Note:** 위 템플릿을 질문 앞에 붙여주면 “원인/해결/대안”을 즉시 제시할 수 있다.

---

## 🧾 마지막 한 줄 요약

> **역할·아키텍처·보안·Runner·연계를 템플릿으로 ‘고정 제출’하면 GitLab 질문 정답률이 폭증한다.**
